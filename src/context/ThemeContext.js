/**
 * Re-export ThemeProvider from providers folder
 * This file exists for backward compatibility with existing imports
 */
export { ThemeProvider, useTheme, lightTheme, darkTheme } from '../app/providers/ThemeProvider';
export { default as ThemeContext } from '../app/providers/ThemeProvider';
