/**
 * Re-export AppProvider from providers folder
 * This file exists for backward compatibility with existing imports
 */
export { AppProvider, useApp } from '../app/providers/AppProvider';
export { default as AppContext } from '../app/providers/AppProvider';
